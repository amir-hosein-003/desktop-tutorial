/**@type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        extend: {
            colors: {
                primary: "var(--color-primary)"
            }
        },
    },
    plugins: []
}